# 1. Port và ID định danh
server.port=${PORT:8080}
server.id=${SERVER_ID:Local_Server}
logging.level.org.springframework.boot.web.embedded.tomcat=DEBUG

# 2. Cấu hình PostgreSQL 
# Đã thêm sslmode=require để đảm bảo kết nối trên Cloud
spring.datasource.url=jdbc:postgresql://dpg-d75p1p9r0fns73f8vvkg-a.singapore-postgres.render.com/booking_db_fq2h?sslmode=require

# Các thông số đăng nhập (Giữ nguyên từ thông tin của bạn)
spring.datasource.username=booking_db_fq2h_user
spring.datasource.password=RJ1fp3SncNHfI0AskR9cpLex59roW60t
spring.datasource.driver-class-name=org.postgresql.Driver

# 3. Cấu hình JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Đã lược bỏ hibernate.dialect để tránh cảnh báo WARN

# 4. Link các node hệ phân tán
peer.servers=${PEER_SERVERS:}

# 5. Cấu hình khác
spring.main.allow-bean-definition-overriding=true